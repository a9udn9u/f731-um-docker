# deployed binaries (rebuild with build/, image/)
linux-um
stub_exe
umnet
passt-bin
umdprobe
*.ext4
*.ext4.gz

# build trees/outputs (build scripts default to /tmp; ignore local ones)
kbuild/
tree/
kbuild.log
*.log

# helper APK intermediates and signing material (never commit the keystore)
app/helper/out/
app/helper/ks.jks

__pycache__/
*.pyc
